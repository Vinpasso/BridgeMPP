<?xml version="1.0" encoding="UTF-8"?>
<!--
To change this license header, choose License Headers in Project Properties.
To change this template file, choose Tools | Templates
and open the template in the editor.
-->


<bridgempp>
    <services>
        <service>
            <type>ConsoleService</type>
            <options></options>
        </service>
        <service>
            <type>SocketService</type>
            <options>1234</options> <!-- Option: Port Number -->
        </service>
<!--
        <service>
            <type>MailService</type>
            <options>(imapserver); (port); (smtpserver); (smtpport); (emailaddress); (password)</options>
        </service>
-->
<!-- 
        <service>
            <type>XMPPService</type>
            <options>(connectserver); (port); (xmppserver); (jid); (password); (status); (oldstyle SSL)</options>
        </service>
-->
<!--
        <service>
            <type>WhatsappService</type>
            <options>(two digit countrycode); (phonenumber with country code without 00 or +); (whatsapp password)</options>
        </service>
-->
<!--
        <service>
            <type>SkypeService</type>
            <options></options>
        </service>
-->
    </services>
</bridgempp>
